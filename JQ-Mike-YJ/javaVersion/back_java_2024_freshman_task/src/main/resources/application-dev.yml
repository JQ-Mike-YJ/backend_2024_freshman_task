server:
#  port: 9020
  # https 加密端口号 443
  port: 443
  ssl:
    # SSL 证书路径 一定要加上 classpath
    key-store: classpath:yangjinlearn.cn.pfx
    # SSL 证书密码
    key-store-password: ziuauxtx
    # 证书类型
    key-store-type: PKCS12
spring:
  application:
    name: back_java_2024_freshman_task
  datasource:
    name: user
    url: jdbc:mysql://47.97.124.224:3306/question_answer
    username: root
    password: YangJin123456@!

logging:
  config: classpath:config/logback-spring.xml
  location: opt/logs
  fileName: ${spring.application.name}-server
  levels: INFO
  sqlPrint: true


# 打印 sql 日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl